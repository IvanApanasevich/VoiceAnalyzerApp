package com.example.voiceanalyzerapp

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.voiceanalyzerapp.R

class SessionPlaceholderActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_session_placeholder)
        // You can add a toolbar with a back button here if desired
    }
}